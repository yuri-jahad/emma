import { actuHandler } from './handler'

export default {
  variants: ['actu', 'news'],
  helper: 'Dernières actualités hip-hop (hiphop.fr · rap2k · Mouv) avec pagination. `.actu`',
  fn: actuHandler
}
